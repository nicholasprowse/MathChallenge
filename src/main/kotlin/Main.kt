fun main() {
    var arr = List<Int>()
    for (i in 0 until 100) {
        arr(i)
    }

    arr.insert(-10, D0)
    println(arr)
    println(arr.length)
    println((D4)(D3)(D7) / (D1)(D3))
    println((D4)(D3)(D7) % (D1)(D3))
    println((D4)(D3)(D7) * (D1)(D3))
    val x = 2
    val array = D0 until (D8)(D3)
    arr(x)(2)(3)
    println(array)

    var i = D0
    while (i lessThan array.length === True) {
        println(array[i])
        i++
    }

    println(array[(D3)(D0)(D0)])
}